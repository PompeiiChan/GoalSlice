# BUG 日志：T-006

**第 1 次 FAIL**（2026-06-24）

## 问题列表

### BUG-1 [阻塞] 真实业务库缺 `clarify_answers_json` 列

- **现象**：`PATCH /api/v1/goals/{id}/clarify` 在运行时 SQLite 上报 `no such column: user_contexts.clarify_answers_json`
- **影响**：AC-4 刷新恢复、P02 分批保存、澄清全流程在真实联调下不可用
- **根因**：`UserContext` ORM 新增字段，但 `init_db.py` 仅 `create_all`，不迁移已有表
- **建议修复**：`init_db.py` 或独立脚本幂等 `ALTER TABLE user_contexts ADD COLUMN clarify_answers_json TEXT`；Developer 输出中提醒复验前执行

### BUG-2 [质量门禁] mypy `no-untyped-def`

- **文件**：`backend/src/api/routes/goals.py` 第 29、44、58 行
- **现象**：`$PY -m mypy backend/src backend/tests` 失败
- **建议修复**：为 `create_goal` / `clarify_goal` / `get_active_goal` 补返回类型注解

## 已通过项（无需返工）

- goals 单测 6/6（隔离库）
- 422 冲突拦截
- 前端 goalService Mock 开关与 vite proxy 配置
- P01/P02 无 `[Mock]` 文案

## 第 2 次验收 PASS — 2026-06-24 23:53

返工后全项通过。`migrations.py` + `on_startup(init_db)` 修复旧库缺列；mypy 全绿；43 pytest passed；旧 schema API smoke 通过。详见 `.sdd/test-reports/test-T-006.md`。
